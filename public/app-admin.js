(async()=>{const {user}=await Athena.requireAuth();if(!user.isAppAdmin){Athena.appShell('app-admin.html','<div class="panel"><h1>App admin</h1><p>Application admin access required.</p></div>');return;}async function render(){const data=await Athena.api('/api/admin/orgs');const tiers=await Athena.api('/api/admin/tiers');Athena.appShell('app-admin.html',`<div class="page-head"><div><h1>Application admin</h1><p class="muted">Manage enterprise tiers and user limits.</p></div></div><section class="panel"><h2>Tier config from .env</h2><pre>${Athena.escapeHtml(JSON.stringify(tiers.tiers,null,2))}</pre></section><section class="section"><h2>Enterprise organizations</h2><div class="list">${data.organizations.map(o=>`<div class="panel"><div class="survey-row"><div><strong>${Athena.escapeHtml(o.name)}</strong><p class="muted">${o.active_member_count}/${o.max_active_users} active users • ${o.status}</p></div><div class="row"><select class="plan" data-id="${o.id}"><option value="enterprise" ${o.plan==='enterprise'?'selected':''}>Enterprise</option><option value="pro" ${o.plan==='pro'?'selected':''}>Pro</option><option value="basic" ${o.plan==='basic'?'selected':''}>Basic</option></select><input class="limit" data-id="${o.id}" value="${o.max_active_users}" style="width:90px"><select class="ostatus" data-id="${o.id}"><option value="active" ${o.status==='active'?'selected':''}>Active</option><option value="inactive" ${o.status==='inactive'?'selected':''}>Inactive</option><option value="trial" ${o.status==='trial'?'selected':''}>Trial</option><option value="canceled" ${o.status==='canceled'?'selected':''}>Canceled</option></select><button class="btn primary save" data-id="${o.id}">Save</button></div></div></div>`).join('')}</div></section>`);Athena.$all('.save').forEach(b=>b.onclick=async()=>{const id=b.dataset.id;await Athena.api(`/api/admin/orgs/${id}`,{method:'PATCH',body:JSON.stringify({plan:Athena.$(`.plan[data-id="${id}"]`).value,maxActiveUsers:Athena.$(`.limit[data-id="${id}"]`).value,status:Athena.$(`.ostatus[data-id="${id}"]`).value})});Athena.toast('Saved.');render();});}render();})();
