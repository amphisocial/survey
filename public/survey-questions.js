(async()=>{await Athena.requireAuth();const id=new URLSearchParams(location.search).get('id');async function render(){const d=await Athena.api(`/api/surveys/${id}`);Athena.appShell('survey-questions.html',`<div class="page-head"><div><h1>Questions</h1><p class="muted">Activate one question at a time for the public survey link.</p></div><a class="btn soft" href="/survey-detail.html?id=${id}">Back</a></div><section class="panel"><h2>Add question</h2><div class="form-grid"><label>Question<input id="qtext"></label><label>Type<select id="qtype"><option value="single_choice">Single choice</option><option value="multi_select">Multi-select</option><option value="tag_cloud">Tag cloud</option></select></label><label>Options<textarea id="qopts" rows="4" placeholder="One option per line"></textarea></label><button class="btn primary" id="add">Add question</button></div></section><section class="section"><div class="list">${d.questions.map(q=>`<div class="panel question-card"><div class="survey-row"><div><strong>${Athena.escapeHtml(q.question_text)}</strong><p class="muted">${q.question_type}</p></div><div class="row"><span class="${Athena.statusClass(q.status)}">${q.status}</span><button class="btn primary act" data-q="${q.id}">Go live</button><button class="btn soft ina" data-q="${q.id}">Inactivate</button><button class="btn soft com" data-q="${q.id}">Complete</button><button class="btn soft share" data-q="${q.id}">Question link</button></div></div></div>`).join('')}</div></section>`);Athena.$('#add').onclick=async()=>{try{await Athena.api(`/api/surveys/${id}/questions`,{method:'POST',body:JSON.stringify({questionText:Athena.$('#qtext').value,questionType:Athena.$('#qtype').value,options:Athena.$('#qopts').value.split('\n').map(x=>x.trim()).filter(Boolean)})});render();}catch(e){Athena.toast(e.message)}};Athena.$all('.act').forEach(b=>b.onclick=()=>Athena.api(`/api/questions/${b.dataset.q}/activate`,{method:'POST'}).then(render));Athena.$all('.ina').forEach(b=>b.onclick=()=>Athena.api(`/api/questions/${b.dataset.q}/inactivate`,{method:'POST'}).then(render));Athena.$all('.com').forEach(b=>b.onclick=()=>Athena.api(`/api/questions/${b.dataset.q}/complete`,{method:'POST'}).then(render));Athena.$all('.share').forEach(b=>b.onclick=async()=>{const r=await Athena.api(`/api/questions/${b.dataset.q}/share-link`,{method:'POST'});navigator.clipboard?.writeText(r.url);Athena.toast('Question link copied.');});}render();})();
