(async()=>{const {user}=await Athena.requireAuth();async function render(){const data=await Athena.api('/api/orgs');Athena.appShell('org-admin.html',`<div class="page-head"><div><h1>Enterprise admin</h1><p class="muted">Create organizations, invite creators, and activate/deactivate users within tier limits.</p></div></div><div class="grid two"><section class="panel"><h2>Create organization</h2><label>Organization name<input id="orgName"></label><button class="btn primary" id="createOrg">Create</button></section><section class="panel"><h2>Your organizations</h2><div class="list">${data.memberships.map(m=>`<div class="card"><strong>${Athena.escapeHtml(m.organization_name)}</strong><p class="muted">${m.role} • ${m.status} • limit ${m.max_active_users}</p><button class="btn soft manage" data-id="${m.organization_id}">Manage users</button></div>`).join('')||'<p class="muted">No enterprise orgs yet.</p>'}</div></section></div><section class="section"><div id="members"></div></section>`);Athena.$('#createOrg').onclick=async()=>{try{await Athena.api('/api/orgs',{method:'POST',body:JSON.stringify({name:Athena.$('#orgName').value})});render();}catch(e){Athena.toast(e.message)}};Athena.$all('.manage').forEach(b=>b.onclick=()=>manage(b.dataset.id));}async function manage(id){const d=await Athena.api(`/api/orgs/${id}/members`);Athena.$('#members').innerHTML=`<div class="panel"><h2>${Athena.escapeHtml(d.organization.name)} users</h2><p class="muted">Active limit: ${d.organization.max_active_users}</p><div class="form-grid"><label>Invite emails<textarea id="inviteEmails" rows="4"></textarea></label><label>Role<select id="inviteRole"><option value="creator">Creator</option><option value="admin">Admin</option><option value="viewer">Viewer</option></select></label><button class="btn primary" id="inviteBtn">Invite/add users</button></div><table class="table" style="margin-top:18px"><thead><tr><th>Email</th><th>Role</th><th>Status</th><th></th></tr></thead><tbody>${d.members.map(m=>`<tr><td>${Athena.escapeHtml(m.email)}</td><td>${m.role}</td><td>${m.status}</td><td><button class="btn soft toggle" data-mid="${m.id}" data-status="${m.status==='active'?'inactive':'active'}">${m.status==='active'?'Deactivate':'Activate'}</button></td></tr>`).join('')}</tbody></table></div>`;Athena.$('#inviteBtn').onclick=async()=>{try{await Athena.api(`/api/orgs/${id}/invite`,{method:'POST',body:JSON.stringify({emails:Athena.$('#inviteEmails').value,role:Athena.$('#inviteRole').value})});manage(id);}catch(e){Athena.toast(e.message)}};Athena.$all('.toggle').forEach(b=>b.onclick=()=>Athena.api(`/api/orgs/${id}/members/${b.dataset.mid}`,{method:'PATCH',body:JSON.stringify({status:b.dataset.status})}).then(()=>manage(id)).catch(e=>Athena.toast(e.message)));}render();})();
