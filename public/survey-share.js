(async()=>{await Athena.requireAuth();const id=new URLSearchParams(location.search).get('id');async function render(){const d=await Athena.api(`/api/surveys/${id}`);Athena.appShell('survey-share.html',`<div class="page-head"><div><h1>Share survey</h1><p class="muted">Generate survey, question, or live-results links and QR codes. Regenerate to create a newer version.</p></div><a class="btn soft" href="/survey-detail.html?id=${id}">Back</a></div><div class="grid two"><section class="panel"><h2>Create share link</h2><p class="muted">Survey link shows the current active question. Live-results link is ideal for projector screens.</p><div class="row"><button class="btn primary" id="surveyLink">Survey link + QR</button><button class="btn soft" id="liveLink">Live results link</button></div><div id="newLink" style="margin-top:18px"></div></section><section class="panel"><h2>Existing links</h2><div class="list">${d.links.map(l=>`<div class="card"><strong>${l.scope}</strong> <span class="muted">v${l.version}</span><p class="muted">${l.is_active?'Active':'Revoked'}</p><div class="row"><code>${l.token}</code><button class="btn soft regen" data-id="${l.id}">Regenerate</button><button class="btn danger revoke" data-id="${l.id}">Unshare</button></div></div>`).join('')||'<p>No links yet.</p>'}</div></section></div>`);async function create(scope){const r=await Athena.api(`/api/surveys/${id}/share-link`,{method:'POST',body:JSON.stringify({scope})});Athena.$('#newLink').innerHTML=`<p><strong>URL:</strong> <a href="${r.url}" target="_blank">${r.url}</a></p><img class="qr" src="${r.qr}"><div class="row"><button class="btn soft" id="copyLink">Copy link</button></div>`;Athena.$('#copyLink').onclick=()=>{navigator.clipboard?.writeText(r.url);Athena.toast('Copied.')}}Athena.$('#surveyLink').onclick=()=>create('survey');Athena.$('#liveLink').onclick=()=>create('live_results');Athena.$all('.revoke').forEach(b=>b.onclick=()=>Athena.api(`/api/share-links/${b.dataset.id}/revoke`,{method:'POST'}).then(render));Athena.$all('.regen').forEach(b=>b.onclick=async()=>{const r=await Athena.api(`/api/share-links/${b.dataset.id}/regenerate`,{method:'POST'});navigator.clipboard?.writeText(r.url);Athena.toast('New link copied.');render();});}render();})();
