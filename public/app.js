(async()=>{
  const {user,usage}=await Athena.requireAuth();
  const surveys=await Athena.api('/api/surveys').catch(()=>({surveys:[]}));
  Athena.appShell('app.html',`<div class="page-head"><div><h1>Dashboard</h1><p class="muted">Create, share, and analyze live surveys.</p></div><a class="btn primary" href="/survey-new.html">Create survey</a></div><div class="grid four"><div class="card"><span class="mini-label">Plan</span><div class="stat">${Athena.escapeHtml(user.planLabel)}</div><p class="muted">${usage.used||0} of ${usage.limit||0} surveys this month</p></div><div class="card"><span class="mini-label">Surveys</span><div class="stat">${surveys.surveys.length}</div><p class="muted">Total owned surveys</p></div><div class="card"><span class="mini-label">Live</span><div class="stat">${surveys.surveys.filter(s=>s.status==='active').length}</div><p class="muted">Currently active</p></div><div class="card"><span class="mini-label">AI</span><div class="stat">Ready</div><p class="muted">Builder and analyst enabled by .env</p></div></div><section class="section"><div class="page-head"><h2>Recent surveys</h2><a class="btn soft" href="/surveys.html">View all</a></div><div class="list">${surveys.surveys.slice(0,8).map(s=>`<a class="panel survey-row" href="/survey-detail.html?id=${s.id}"><div><strong>${Athena.escapeHtml(s.title)}</strong><p class="muted">${s.question_count||0} questions • ${s.response_count||0} responses</p></div><span class="${Athena.statusClass(s.status)}">${s.status}</span></a>`).join('')||'<div class="panel"><p>No surveys yet.</p><a class="btn primary" href="/survey-new.html">Create your first survey</a></div>'}</div></section>`);
})();
